import { Router } from 'express';

const router = Router();

router.post(
    '/login',
    (req, res, next) => {
        try {
        } catch (error) {
        } finally {
            next();
        }
    }
)